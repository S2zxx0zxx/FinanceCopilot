apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fincopilot.fullname" . }}-test-connection"
  labels:
    {{- include "fincopilot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fincopilot.fullname" . }}-frontend:{{ .Values.frontend.service.port }}']
  restartPolicy: Never
